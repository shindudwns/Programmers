class Solution {
    public String solution(int n) {
        String[] arr ={"1","2","4"};
        String answer = "";
        int first=n/3;
        int last=n%3;
        if (n==0){
            int a=first+ "";
            System.out.println(a);
            // answer+=integer.tostring(first-1);
            // answer+=arr[last];
        }
        // else{
        //     answer+=integer.tostring(first);
        //     answer+=arr[last];
        // }
        
        return answer;
    }
}
